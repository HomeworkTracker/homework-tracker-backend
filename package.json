{
  "name": "homework-tracker-backend",
  "version": "1.0.0",
  "description": "Homework Tracker Backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r build/* && tsc && node build/index.js",
    "build:live": "nodemon -w src/* --exec \"npm run build\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.12.11",
    "express": "^4.19.2",
    "nodemon": "^3.1.0",
    "typescript": "^5.4.5"
  }
}
